import React from 'react';
import { Alert } from 'react-bootstrap';

const Home: React.FC = () => {
  return (
    <div className="container">
      <br />
      <Alert variant="primary text-center">Todo do List App Example</Alert>
    </div>
  );
};

export default Home;
